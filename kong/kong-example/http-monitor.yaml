apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  annotations:
    prometheus.io/scrape: "true"
  name: http-servicemonitor
  labels:
    release: promstack
spec:
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: http-svc
      enable-metrics: "true"
  endpoints:
  - port: http